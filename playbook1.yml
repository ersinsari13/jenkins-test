- name: test playboook
  become: true
  hosts: server
  tasks:
    - name: ping
      ansible.builtin.ping:
    - name: debug
      ansible.builtin.command: echo ${PASSWORD} ${USERNAME} > /home/ec2-user/ersin.txt
    - name: shell
      ansible.builtin.shell: echo ${PASSWORD} ${USERNAME} > /var/lib/jenkins/ersin.txt
